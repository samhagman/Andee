# Environment Configuration
#
# This project uses two env files:
#   .dev.vars  - Local development (wrangler dev reads this)
#   .prod.env  - Production secrets (deployed via npm run deploy-secrets)
#
# Copy this file to both .dev.vars and .prod.env, then fill in values.

# Telegram bot token from @BotFather
BOT_TOKEN=your_telegram_bot_token_here

# API key for authenticating with the sandbox worker
# Generate with: openssl rand -hex 16 | sed 's/^/adk_/'
# Must match the key in claude-sandbox-worker/.dev.vars
ANDEE_API_KEY=adk_your_key_here

# Comma-separated Telegram user IDs allowed to use the bot
# Find your ID: message @userinfobot on Telegram, or check bot logs
# Leave empty to allow all users (recommended for .dev.vars)
ALLOWED_USER_IDS=
